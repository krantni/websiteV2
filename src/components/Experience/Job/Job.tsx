import * as React from 'react'

interface JobProps {
  company: string;
  title: string;
  startDate: string;
  endDate: string;
  desc: string;
}

const Job = (props:JobProps) => {
  return (
    <>
    </>
  )
}

export default Job;